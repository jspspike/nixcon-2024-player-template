[package]
name = "nixcon-2024-player-template"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.7"
tokio = { version = "1.41.0", features = ["rt-multi-thread", ]}
uuid = {"1.11.0", features = ["v4", "fast-rng"]}
